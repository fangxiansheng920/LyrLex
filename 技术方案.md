# 歌词单词学习器 — 技术方案 v1.0

> 状态：**M1–M6 已完成；当前版本 0.1.2（产品名 LyrLex）**
> 关联文档：`歌词单词学习器.md`（原始想法与讨论）
> 本文档汇总所有已拍板决策，并给出完整的架构、数据模型、接口与里程碑设计。

---

## 0. 已确认决策清单

| # | 决策项 | 结论 |
| --- | --- | --- |
| D1 | 产品定位 | 个人本地学习工具，无登录，单机运行 |
| D2 | 生词本形态 | 纯收藏：英/日分区、搜索、筛选、看详情、放发音、删除，**不做复习/SRS** |
| D3 | API 源 | 有道文本翻译(NMT) 优先（设置内配置应用 ID/密钥即时生效），未配置或失败回退免费源 MyMemory；发音用有道免费 dictvoice 接口 |
| D4 | 歌曲搜索 | 在线搜索（NeteaseCloudMusicApi）+ 手动粘贴/上传兜底 |
| D5 | 第一版语言 | **英文、日文一起做** |
| D6 | 点词展示 | Popover 弹窗（原词+基本形、读音、词义列表、小喇叭、加入生词本） |
| D7 | 整句翻译展示 | 每句歌词下方一行小字 |
| D8 | 主题 | 四套预设渐变 + 自定义纯色，默认白色 |
| D9 | 双语歌词 | 粘贴内容自带翻译时优先用自带翻译，不再调 API |
| D10 | 数据存储 | SQLite + 音频文件，全部放在 Electron `userData` 目录，设置里提供「打开数据目录」 |
| D11 | 前端框架 | Vue 3 + TypeScript + Vite |
| D12 | 依赖注入 | wire + 接口模式：service 定义 interface，实现 struct + 构造函数 + `wire.Bind` 绑定（见 §4.4） |
| D13 | 搜索方案 | 不引入 Elasticsearch（过重）；生词/歌曲搜索用 SQLite LIKE，个人量级足够 |
| D14 | 外观自定义 | 支持自定义背景图片（本机保存）与背景透明度，以及按钮主色、渐变段色 |
| D15 | 页面状态 | 模块切换用 `<KeepAlive>` 缓存各页，切回保留上次内容（生词本/搜索页按需刷新数据） |

---

## 1. 产品概述

**一句话**：导入/搜索歌词，逐句自动翻译，点击歌词中的任意单词即可查看读音、词义并一键收藏——支持英文与日文。

**核心价值**：把「听歌学语言」变成可交互的查词体验；生词本保留完整语境（哪首歌、哪一句、当时的翻译）。

**核心模块**：

1. 歌词学习
2. 生词本（纯收藏）
3. 歌曲搜索
4. 设置中心

---

## 2. 功能需求

### 2.1 歌词学习

- 输入方式：文本框粘贴歌词 / 本地文件上传（`.txt` `.lrc`）。
- 语言识别：自动识别英文 / 日文（含双语歌词场景）。
- 分句与翻译：
  - 自动按行分句。
  - 每句下方以小字显示中文翻译（D7）。
  - 若原歌词自带中文翻译（双语 LRC 等），优先使用自带翻译（D9）。
- 点击单词（D6）：
  - 英文词：显示原词、基本形（词形还原）、IPA 音标、词义列表、发音喇叭。
  - 日文词：显示原词、基本形、假名、罗马音、声调（有数据才显示）、词义列表、发音喇叭。
  - 词义列表按「当前语境匹配度」排序，用户可勾选具体词义。
  - 「加入生词本」默认以**基本形**入库，原句自动存为例句；允许用户改为「以原形入库」。
- 日文特殊处理：
  - 使用形态分析器（kagome）分词，不按空格切分。
  - 连续的功能性 token（动词+助动词/补助动词，如「行ってしまった」）合并为一个可点击词组，整体显示读音与基本形。
  - 助词、标点只显示读音，**不可**加入生词本。

### 2.2 生词本（纯收藏）

- 英/日两个独立分区入口，底层各自一张表。
- 功能：按语言筛选、关键词搜索、查看详情（词义列表 + 例句 + 来源歌曲）、播放发音、删除。
- 每条生词可回溯例句：展示来源歌曲、原句、整句翻译，可跳转到该歌词。
- 无复习功能、无到期提醒（D2）。

### 2.3 歌曲搜索

- 输入歌名 / 歌手，调用在线歌词源搜索（D4）。
- 搜索结果列表 → 选择后拉取歌词 → 进入歌词学习界面。
- 在线源不可用时自动降级：提示「粘贴或上传歌词」。
- 已导入的歌曲保存到本地歌曲库，可再次打开、按关键词搜索本地库。

### 2.4 设置中心

- 主题：四套预设渐变 + 自定义渐变色（三段色）+ 自定义纯色 + 按钮颜色自由输入（D8）。
- 外观：支持自定义背景图片，并可调整背景透明度（D14）。
- API 配置：有道「文本翻译（NMT）」应用 ID / 密钥，填写后即时生效，失败或留空回退免费源；百度 / DeepL 暂为预留。
- 发音源切换：目前内置有道 dictvoice。
- 数据：显示数据目录路径，提供「打开数据目录」按钮（D10）。

### 2.5 非功能需求

- 无登录、无多用户，所有数据本地。
- 离线优先：已查询过的词、已缓存的音频、已导入的歌词均可离线使用。
- 单人使用，无需高并发设计；但查询必须有缓存，避免重复调用外部 API。

---

## 3. 技术选型

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 桌面外壳 | Electron | 原方案指定；跨平台，生态成熟 |
| 前端框架 | **Vue 3 + TypeScript + Vite**（已确认，见 D11） | 组件化适合歌词 token 渲染 |
| 后端 | Go + Gin | 原方案指定；单二进制易打包 |
| ORM | GORM | 原方案指定；未来可平滑迁移 MySQL/PG |
| 配置 | Viper + `.env` | 原方案指定；`.env` 存 API key 等敏感数据 |
| 依赖注入 | wire（`wire.Bind` 接口绑定，见 D12/§4.4） | 原方案指定；service 以接口 + 构造函数装配 |
| 数据库 | **SQLite** | 零安装、单文件、备份=复制；生词/歌曲搜索用 LIKE，个人量级足够（D13） |
| 缓存 | 进程内存 + SQLite 缓存表 | 不需要 Redis |
| 英文词典 | Free Dictionary API（在线）+ 百度翻译 sug 兜底 | 免费无 key；ECDICT 本地词典为后续计划 |
| 日文分词 | kagome + UniDic（纯 Go，离线） | 无需外部安装 MeCab |
| 整句翻译 | 有道文本翻译(NMT) 优先，回退 MyMemory（免费） | 设置内配置 key 即时生效 |
| 发音 | 有道 dictvoice（en 美音 / ja 日文） | 免费无 key，音频本地缓存 |
| 歌词搜索 | NeteaseCloudMusicApi（非官方，Provider 封装） | 免费；不稳定可降级 |
| 打包 | electron-builder + Go 二进制作为 extraResources | 单安装包交付 |

---

## 4. 系统架构

### 4.1 进程架构

```
┌─────────────────────────────────────────────┐
│ Electron                                     │
│  ┌──────────────┐   IPC   ┌───────────────┐  │
│  │  Renderer    │◄──────►│  Main Process │  │
│  │  Vue 3 UI    │         │  启动/托管 Go  │  │
│  └──────┬───────┘         └──────┬────────┘  │
│         │ HTTP (localhost)       │ spawn     │
│         ▼                        ▼           │
│  ┌──────────────────────────────────────┐    │
│  │  Go 后端 (sidecar 进程)               │    │
│  │  Gin HTTP server @ 127.0.0.1:<port> │    │
│  └──────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
         │
         ├── SQLite (userData/data.db)
         ├── audio cache (userData/audio/)
         └── 外部 API (翻译/词典/发音/歌词)
```

- Electron Main 启动时拉起 Go 侧车进程，Go 监听 `127.0.0.1` 随机空闲端口，端口通过启动参数/环境变量回传。
- Renderer 通过 HTTP 访问 Go 后端；Main 负责 Go 进程生命周期（退出时关闭）。
- 健康检查：`GET /healthz`，就绪后 UI 才允许请求。

### 4.2 后端分层架构

```
handler → service → dao → SQLite
            │
            └── provider/  (翻译、词典、分词、发音、歌词 — 统一接口)
```

- **handler**：HTTP 参数校验（req + validator）、调用 service、返回 resp。
- **service**：业务逻辑、缓存策略、Provider 编排；每个 service 先定义 interface，再写实现 struct，构造函数注入依赖，并配套 wire ProviderSet（模板见 §4.4）。
- **dao**：GORM 数据访问，不出现业务逻辑。
- **model / req / resp**：数据结构分离。
- **provider**：所有外部能力抽象为接口，可替换、可降级。
- **middleware**：日志、错误处理、recovery。
- **config**：Viper 读取 `configs/config.yaml` + `.env`。

### 4.3 Provider 接口（核心抽象）

```go
// 整句翻译
type TranslationProvider interface {
    Name() string
    Translate(ctx context.Context, text, source, target string) (string, error)
}

// 词典查询（词形还原 + 音标 + 释义 + 音频地址）
type DictionaryProvider interface {
    Name() string
    Lookup(ctx context.Context, lang Language, word string) (*DictEntry, error)
}

type DictEntry struct {
    Surface   string     // 查询原词
    Lemma     string     // 基本形
    POS       string     // 词性
    Phonetic  string     // 英文 IPA
    Kana      string     // 日文假名
    Romaji    string     // 日文罗马音
    Accent    string     // 日文声调（可空）
    Meanings  []Meaning  // 词义列表
    AudioURLs []string   // 音频源地址（可空）
    Source    string     // 数据来源标识
}

// 分词（英文 tokenizer / 日文形态分析）
type TokenizerProvider interface {
    Name() string
    Tokenize(ctx context.Context, lang Language, line string) ([]Token, error)
}

type Token struct {
    Surface  string // 原形文本
    Lemma    string // 基本形
    POS      string // 词性
    Reading  string // 读音（日文假名/英文音标）
    Accent   string // 声调（可空）
    GroupID  int    // 合并词组 ID（日文功能词合并；0 = 独立）
    Addable  bool   // 是否可加入生词本（助词/标点为 false）
}

// 发音
type PronunciationProvider interface {
    Name() string
    Fetch(ctx context.Context, lang Language, word string) ([]byte, error) // 返回音频二进制
}

// 歌词搜索
type LyricsProvider interface {
    Name() string
    Search(ctx context.Context, query string) ([]SongMeta, error)
    GetLyrics(ctx context.Context, songID string) (*Lyrics, error)
}
```

默认实现组合：

| Provider | 默认实现 | 兜底顺序 |
| --- | --- | --- |
| TranslationProvider | CompositeTranslationProvider（Youdao 优先 + MyMemory 回退） | 有道未配置/失败时回退免费源 |
| DictionaryProvider (en) | FreeDictionaryProvider（dictionaryapi.dev）→ 百度翻译 sug 兜底 | 免费无 key；ECDICT 本地词典为后续计划 |
| DictionaryProvider (ja) | JishoProvider（在线，释义自动转中文）；不可达时 kagome 本地兜底假名/罗马音 | 在线优先、本地兜底 |
| TokenizerProvider (en) | EnglishTokenizer（正则 + 缩写处理 + snowball 词形） | — |
| TokenizerProvider (ja) | JapaneseTokenizer（kagome/UniDic）+ 功能词合并后处理 | — |
| PronunciationProvider | YoudaoDictVoiceProvider（en 美音 / ja 日文） | 免费无 key，本地缓存；Edge TTS 为后续计划 |
| LyricsProvider | NeteaseCloudMusicApiProvider | 失败降级为手动导入 |

### 4.4 service + wire 代码风格（参考模板）

每个 service 严格遵循以下格式（以歌词服务为例）：

```go
// service/lyric.go

type LyricService interface {
    Parse(ctx context.Context, text string) (*ParseResult, error)
    Translate(ctx context.Context, source, target string, lines []string) ([]string, error)
    Tokenize(ctx context.Context, lang model.Language, line string) ([]provider.Token, error)
}

type lyricService struct {
    dao   dao.LyricDao
    trans provider.TranslationProvider
    tok   provider.TokenizerProvider
    cfg   *config.Config
}

var _ LyricService = (*lyricService)(nil)

func NewLyricService(
    d dao.LyricDao,
    trans provider.TranslationProvider,
    tok provider.TokenizerProvider,
    cfg *config.Config,
) *lyricService {
    return &lyricService{
        dao:   d,
        trans: trans,
        tok:   tok,
        cfg:   cfg,
    }
}

var LyricProviderSet = wire.NewSet(
    NewLyricService,
    wire.Bind(new(LyricService), new(*lyricService)),
)
```

约定：

- handler 依赖 **service 接口**（`LyricService`），不依赖实现 struct。
- 每个 service 在其包内定义自己的 `XxxProviderSet`；dao、provider、config 同样各带 ProviderSet，最终在 `wire.go` 聚合生成 `wire_gen.go`。
- 构造函数只做依赖注入，不包含业务逻辑；`var _ LyricService = (*lyricService)(nil)` 用于编译期保证实现满足接口。

---

## 5. 目录结构

```
歌词单词学习器/
├── 歌词单词学习器.md          # 原始想法
├── 技术方案.md               # 本文档
├── backend/                  # Go 后端
│   ├── cmd/server/main.go
│   ├── internal/
│   │   ├── config/           # Viper 配置加载
│   │   ├── handler/          # HTTP 层
│   │   ├── service/          # 业务层（interface + 实现 + XxxProviderSet）
│   │   ├── dao/              # 数据访问层
│   │   ├── model/            # GORM 模型
│   │   ├── req/              # 请求结构 + validate tag
│   │   ├── resp/             # 响应结构
│   │   ├── provider/         # Provider 接口与实现
│   │   │   ├── translation/
│   │   │   ├── dictionary/
│   │   │   ├── tokenizer/
│   │   │   ├── pronunciation/
│   │   │   └── lyrics/
│   │   ├── middleware/       # 日志、错误、recovery
│   │   ├── router/           # 路由注册
│   │   ├── wire.go           # wire 依赖装配入口（聚合各 ProviderSet）
│   │   └── wire_gen.go       # wire 生成文件（go generate 产出）
│   ├── configs/
│   │   └── config.yaml       # 非敏感配置
│   ├── .env.example          # 敏感配置模板
│   └── go.mod
├── frontend/
│   ├── electron/
│   │   ├── main.ts           # 主进程：拉起 Go sidecar
│   │   └── preload.ts
│   ├── src/
│   │   ├── pages/            # 歌词学习/生词本/歌曲搜索/设置（.vue）
│   │   ├── components/       # LyricLine、WordToken、WordPopover 等（.vue）
│   │   ├── api/              # HTTP 客户端
│   │   └── theme/            # 主题变量与切换
│   └── package.json
└── data/                     # 开发期本地数据（发布后位于 userData）
```

---

## 6. 数据模型

GORM 模型（SQLite）：

```go
type Song struct {
    ID        int64     `gorm:"primaryKey"`
    Title     string    `gorm:"index"`
    Artist    string    `gorm:"index"`
    Language  string    // "en" | "ja"
    Source    string    // "online" | "manual"
    CreatedAt time.Time
}

type SongLine struct {
    ID            int64  `gorm:"primaryKey"`
    SongID        int64  `gorm:"index"`
    LineIndex     int
    Text          string // 原始行
    TranslationZH string // 整句翻译（可空）
    TokensJSON    string // 分词结果缓存
}

type EnglishWord struct {
    ID         int64     `gorm:"primaryKey"`
    Word       string    `gorm:"uniqueIndex"` // 入库词（默认基本形）
    Lemma      string    // 词典基本形
    PhoneticUK string    // 英式 IPA
    PhoneticUS string    // 美式 IPA
    AudioPath  string    // 本地音频缓存路径（可空）
    CreatedAt  time.Time
}

type JapaneseWord struct {
    ID        int64     `gorm:"primaryKey"`
    Word      string    `gorm:"uniqueIndex"` // 入库词（默认基本形）
    Lemma     string    // 基本形
    Kana      string    // 假名
    Romaji    string    // 罗马音
    Accent    string    // 声调（可空）
    POS       string    // 词性
    AudioPath string    // 本地音频缓存路径（可空）
    CreatedAt time.Time
}

type WordSense struct {
    ID       int64  `gorm:"primaryKey"`
    WordType string `gorm:"index"` // "en" | "ja"
    WordID   int64  `gorm:"index"`
    POS      string
    Meaning  string // 中文释义
    Source   string // 来源词典/API
}

type Example struct {
    ID            int64 `gorm:"primaryKey"`
    WordType      string `gorm:"index"` // "en" | "ja"
    WordID        int64  `gorm:"index"`
    SenseID       int64  // 用户勾选的词义（可空）
    SongID        int64  `gorm:"index"`
    LineIndex     int
    Text          string // 原句
    TranslationZH string // 原句翻译
}
```

缓存表：

```go
type WordCache struct {        // 词查询缓存
    Lang      string `gorm:"uniqueIndex:idx_lang_word"`
    Word      string `gorm:"uniqueIndex:idx_lang_word"`
    Payload   string // DictEntry JSON
    UpdatedAt time.Time
}

type TranslationCache struct { // 整句翻译缓存
    Hash      string `gorm:"uniqueIndex"` // sha256(source+target+text)
    Source    string
    Target    string
    Text      string
    Result    string
    UpdatedAt time.Time
}
```

说明：

- **英文与日文分两张表**：字段差异大，避免大量空列；共享 `WordSense` 与 `Example` 实现统一的词义与语境。
- **搜索**：生词本与本地歌曲搜索用 SQLite `LIKE`（词形/假名/标题/歌手）；个人量级足够，不引入 Elasticsearch（D13）。
- **去重**：同语言下 `word` 唯一；默认以基本形入库，因此 `waiting/waited/waits` 只会产生一条 `wait`，原形保存在 `Example.Text` 的句子里。
- **歌曲去重**：保存/导入时按「歌名 + 歌手」匹配，已存在则覆盖其歌词行（保留原 ID，语境引用不失效）；歌名留空（未命名歌曲）时不去重。
- **迁移**：使用 GORM AutoMigrate + 版本化 SQL 迁移文件（`backend/migrations/`）。

---

## 7. 后端 API 设计

统一前缀 `/api`，本地 HTTP，无需鉴权（仅绑定 127.0.0.1）。

| Method | Path | 说明 |
| --- | --- | --- |
| GET | `/healthz` | 健康检查 |
| POST | `/lyrics/parse` | 解析歌词文本/文件 → 语言、行数组 |
| POST | `/lyrics/translate` | 批量翻译行（带缓存），body: `{source, target, lines[], force?}`；`force=true` 跳过缓存重翻 |
| POST | `/lyrics/tokenize` | 对一行分词（带 tokens_json 缓存），body: `{language, line}` |
| POST | `/words/lookup` | 查词详情：`{language, word, context?, force?}` → 基本形/音标/假名/词义/音频；`force=true` 跳过缓存重查 |
| GET | `/words/audio/{language}/{word}` | 返回音频文件（优先本地缓存，无则拉取并缓存） |
| POST | `/vocabulary` | 加入生词本：`{language, word, lemma?, sense_id?, song_id?, line_index?}` |
| DELETE | `/vocabulary/{language}/{id}` | 删除生词 |
| GET | `/vocabulary` | 列表：`?language=&q=&song_id=` |
| GET | `/vocabulary/{language}/{id}` | 详情：词义列表 + 例句 + 来源歌曲 |
| POST | `/songs` | 保存歌曲与歌词（含翻译、分词缓存）；同一「歌名+歌手」已存在则覆盖歌词，不重复新增 |
| GET | `/songs` | 本地歌曲库：`?q=` |
| GET | `/songs/{id}/lyrics` | 获取歌词（含翻译与分词结果） |
| POST | `/songs/search` | 在线搜索歌名/歌手 → 歌曲列表（LyricsProvider） |
| POST | `/songs/{id}/import` | 从在线源拉取歌词并保存 |
| DELETE | `/songs/{id}` | 删除本地歌曲及其歌词行 |
| POST | `/songs/batch-delete` | 批量删除歌曲，body: `{ids:[...]}` |
| GET | `/settings` | 读取设置 |
| PUT | `/settings` | 更新设置（主题、API key、发音源） |
| GET | `/settings/datadir` | 返回数据目录路径（用于「打开数据目录」） |
| POST | `/settings/test-translation` | 用当前密钥测试有道翻译，返回译文或错误码（108/110/202/401/411 等） |

错误响应统一结构：`{code, message, data}`；校验错误返回 422 与字段错误明细。

---

## 8. 关键流程

### 8.1 导入歌词

1. 用户粘贴文本或选择文件 → `POST /lyrics/parse`。
2. 后端检测语言（en/ja），按行拆分；若检测到双语行，记录自带翻译（D9）。
3. 返回行数组，前端逐行渲染；`POST /lyrics/translate` 批量翻译缺失翻译的行并缓存。
4. 前端逐行请求 `POST /lyrics/tokenize`，得到 token 数组并渲染为可点击词块（日文含合并词组）。
5. 用户选择「保存歌曲」时 `POST /songs` 持久化（含翻译与 tokens_json）。

### 8.2 点击单词

1. 点击 token → 携带原词与所在句 `context` 调 `POST /words/lookup`。
2. 后端按语言走 Dictionary 链：本地词典 → 在线词典 → 翻译 API 兜底；结果写入 `word_cache`。
3. 返回基本形、读音、词义列表（按语境排序）、音频可用性；**非中文释义自动翻译成中文**（有道优先，失败回退免费源）。
4. 前端 Popover 展示；点小喇叭 → `GET /words/audio/{language}/{word}`，后端命中本地缓存则直接返回，否则走发音 Provider 链并缓存。

### 8.3 加入生词本

1. 用户在 Popover 勾选词义（可多选/不选），默认以基本形入库，可切换为原形入库。
2. `POST /vocabulary`：写入/更新 `EnglishWord` 或 `JapaneseWord`（word 唯一）；写入 `WordSense`（勾选的词义）；写入 `Example`（song_id + line_index + 原句 + 翻译）。
3. 前端生词本即时可见该词及其语境。

### 8.4 歌曲搜索

1. 输入关键词 → `POST /songs/search` 走 `LyricsProvider`。
2. 命中则展示歌曲列表；选择后 `POST /songs/{id}/import` 拉取歌词并保存，进入歌词学习界面。
3. Provider 失败或超时 → 返回可识别错误码，前端提示并引导粘贴/上传（D4）。

---

## 9. 缓存与离线策略

| 数据 | 位置 | 策略 |
| --- | --- | --- |
| 词查询结果 | `word_cache` 表 | TTL 30 天；命中直接返回；词义卡片 ↻ 可强制重查（`force=true` 跳过缓存） |
| 整句翻译 | `translation_cache` 表 | 按 sha256(source+target+text) 永久缓存；歌词学习页每行可点 ↻ 强制重翻（`force=true` 跳过缓存） |
| 分词结果 | `SongLine.tokens_json` | 随歌曲持久化，重复打开不再分词 |
| 音频 | `userData/audio/{lang}/{hash}` | 首次拉取后永久缓存，缺失时重新拉取 |
| 歌词 | `Song` + `SongLine` | 导入即持久化，离线可打开 |

离线能力：已缓存/已导入的词、翻译、音频、歌词均可离线使用；仅在线搜索和未命中缓存的查询需要联网。

---

## 10. 词形还原与分词方案

### 10.1 英文

- MVP 分词：正则 tokenize + 缩写处理（`don't → do not`、`I'm → I am`、`'s` 归属处理）。
- 词形还原：优先查 ECDICT 词形表得到基本形；未命中则用英语词干还原（`snowball`）兜底。
- 音标：Free Dictionary API 提供 IPA（ECDICT 本地词典为后续计划，尚未接入）。

### 10.2 日文

- 分词：kagome + UniDic（纯 Go，离线），输出 surface / lemma / pos / reading。
- 合并规则（MVP 简单版）：
  - 助词（`は/を/が/に/で/と/も/へ` 等）→ 独立展示，可显示读音，`Addable=false`。
  - 标点/记号线 → `Addable=false`。
  - 「动词/形容词 + 助动词/补助动词」连续序列 → 合并为一个词组（如 `行って + しまった → 行ってしまった`），GroupID 相同，点击显示整体读音，基本形取主动词基本形。
- 声调（アクセント）：UniDic 有数据则显示；无则留空（D 级降级，见风险表）。
- 释义：Jisho.org 免费 API（英文释义自动转中文）；不可达时用 kagome 本地分词兜底假名/罗马音。

---

## 11. 音频方案

优先级链（按语言）：

- 英文：有道 dictvoice（`type=2` 美音）。
- 日文：有道 dictvoice（`le=jap`）。
- Free Dictionary 音频、Edge TTS 为后续计划，当前未接入。
- 所有音频首次获取后缓存到 `userData/audio/`，文件名 `sha256(lang+word+source).mp3`。
- 无音频不显示喇叭（D 级降级）。

---

## 12. 风险与降级策略

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 日文声调数据源少 | 声调可能大面积缺失 | 有声调才显示；保证假名+罗马音完整；后续可评估 OJAD 等专用数据 |
| 歌词搜索非官方接口不稳定 | 搜索功能可能临时不可用 | Provider 封装 + 手动导入兜底 + 错误提示 |
| 免费翻译 API 有额度/QPS 限制 | 翻译变慢或失败 | 逐行批量请求、永久缓存、失败重试；配置有道 key 后自动优先有道、失败回退免费源 |
| 日文合并规则不完美 | 个别词组切分/合并不准 | MVP 先覆盖常见助动词组合；分词结果缓存，规则可迭代升级 |
| 词形还原不完美 | 个别词基本形不对 | 词典优先 + lemmatizer 兜底；生词本允许用户手动改词条 |
| 非官方 API 的合规性 | 版权/服务条款风险 | 仅个人本地使用，不做公开分发与服务；接口做成可替换 |

---

## 13. 里程碑计划

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| M1 项目骨架 | 目录结构、Electron↔Go 通信、SQLite 初始化、主题系统、路由/中间件 | Electron 启动后 Go 健康检查通过；四套主题可切换 |
| M2 歌词学习-英文 | 导入/分句/翻译/英文分词/点词 Popover/音频/入生词本 | 粘贴英文歌词可完整走通「查词→发音→收藏」 |
| M3 歌词学习-日文 | kagome 集成、功能词合并、假名/罗马音、日文点词 | 粘贴日文歌词可点词查看假名/罗马音并收藏 |
| M4 生词本 | 英日分区、搜索（LIKE）、详情、例句回溯、删除 | 生词可搜索、可回溯到来源歌词 |
| M5 歌曲搜索 | NeteaseCloudMusicApi Provider、本地歌曲库、降级提示 | 可在线搜歌导入；断网时提示手动导入 |
| M6 设置与打包 | API key 配置、发音源切换、数据目录、electron-builder 打包 | 产出可安装/便携版，数据全在 userData |

> 截至版本 0.1.2，M1–M6 全部完成；产品名 LyrLex，打包产物为 `LyrLex <版本>.exe`（便携）与 `LyrLex Setup <版本>.exe`（安装）。
---

## 14. 打包与部署

- `go build` 产出 `lyrics-server.exe`（Windows）/对应平台二进制。
- electron-builder 将 Go 二进制放入 `extraResources`，Main 进程启动时定位并 spawn。
- SQLite 驱动使用 CGO-free 方案（`glebarez/sqlite` 或 `mattn/go-sqlite3` 按打包便利性选型），避免目标机器依赖。
- 发布形态：Windows 优先（NSIS 安装包 + portable 便携版）；后续可扩展 macOS。
- 数据目录：`app.getPath('userData')`，升级不覆盖数据。

---

## 15. 测试策略

- Go 单元测试：service 层（缓存、编排逻辑）、tokenizer（英文缩写/日文合并规则）、provider（用 mock 外部 API）。
- 集成测试：SQLite dao 层 CRUD、歌词导入→分词→收藏链路。
- 前端：Vitest + Vue Test Utils 做关键组件测试（WordToken 渲染、Popover 交互），MVP 阶段不做完整 E2E。
- 手动验收：按里程碑验收标准逐项过。

---

## 16. 已确认事项（T）

| # | 事项 | 默认 | 状态 |
| --- | --- | --- | --- |
| T1 | 前端框架 | Vue 3 + TypeScript + Vite | ✅ 已确认（D11） |
| T2 | 英文词形还原库 | 词典优先 + `snowball` 词干兜底（golem 模块不可用，已替换） | ✅ 已确认 |
| T3 | SQLite 驱动 | `glebarez/sqlite`（纯 Go，无 CGO） | ✅ 已确认 |
| T4 | 在线歌词源 | NeteaseCloudMusicApi | ✅ 已确认 |
| T5 | 日文声调后续方案 | 第一版留空，二期评估 OJAD | ✅ 已确认 |

> T1–T5 全部确认，技术方案已冻结。等待用户指令后进入开发。
